version: '3.8'

services:
  webcam-face-effects:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webcam-face-effects
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
